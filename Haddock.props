<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

	<PropertyGroup>
		<Configuration Condition="'$(Configuration)' == ''">Debug</Configuration>
		<Version Condition="'$(Version)'==''">0.1.0.0</Version>
	</PropertyGroup>

	<PropertyGroup>
		<RootFolder>$(MSBuildProjectDirectory)</RootFolder>
		<SourceFolder>$(RootFolder)</SourceFolder>
		<SolutionFile>$(SourceFolder)\Haddock.sln</SolutionFile>
		<TestOutputFolder>$(SourceFolder)\Haddock.Web.Tests\bin\$(Configuration)</TestOutputFolder>
	</PropertyGroup>

</Project>